# Copy this to .env and fill in your values
# Never commit .env to git

DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/smriti
SECRET_KEY=generate-with-python3-c-import-secrets-print-secrets.token_hex-32
SERVER_URL=http://192.168.1.100:8000
# SERVER_URL = your laptop's local IP (run: ifconfig on Mac, ipconfig on Windows)
# This is used to build the audio file URLs sent to the patient phone
